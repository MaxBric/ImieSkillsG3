{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
  <h2>Ajout d'un skill</h2>
  <a href="{{ app.request.headers.get('referer') }}">Retour</a>
  <hr/>
  <form method="post" action="{{ path('imie_skills_skill_add') }}" {{ form_enctype(form) }}>
    {{ form(form) }}
  </form>
{% endblock article %}
