{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
<h2><span class="glyphicon glyphicon-knight"></span> Modification de la compétence</h2>
<a href="{{ path('imie_skills_skill_modify'), {id: skill.id}}}"><span class="glyphicon glyphicon-menu-left"> Retour</a>
<form method="post" action="{{ path('imie_skills_skill_modify', {id: id}) }}" {{ form_enctype(form) }}>
  {{ form(form) }}
</form>
{% endblock article %}
