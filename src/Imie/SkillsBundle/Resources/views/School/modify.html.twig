{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
    <h2><span class="glyphicon glyphicon-user"></span> Modification de l'utilisateur</h2>
    <hr/>
    {#<a class="btn btn-primary" href="{{ path('imie_skills_school_me') }}"><span class="glyphicon glyphicon-menu-left"></span> Retour</a>#}
    <hr/>
    <form method="post" action="{{ path('imie_skills_school_modify', {id:id}) }}" {{ form_enctype(form) }}>
        {{ form(form) }}
    </form>
{% endblock article %}
