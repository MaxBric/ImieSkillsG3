{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
<h2>Modification de l'étape</h2>
<a href="{{ path('imie_skills_state_modify', {id: state.id}) }}"><span class="glyphicon glyphicon-menu-left"> Retour</a>
<hr/>
<form method="post" action="{{ path('imie_skills_state_modify', {id: id}) }}" {{ form_enctype(form) }}>
  {{ form(form) }}
</form>
{% endblock article %}
