{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
<h2><span class="glyphicon glyphicon-tasks"></span> Liste des étapes</h2>
<hr/>
<a href="{{ path('imie_skills_state_add') }}"><span class="glyphicon glyphicon-plus"></span> Ajouter une étape</a>
<hr/>
<table class="table table-striped">
  <thead>
    <th>Nom</th>
  </thead>
  <tbody>
    {% for state in states if states is defined %}
    <tr>
      <td>{{ state.statut }}</a></td>
      <td><a href="{{ path('imie_skills_state_modify', {id:state.id}) }}"><span class="glyphicon glyphicon-cog"></span></a></td>
      <td><a href="{{ path('imie_skills_state_delete', {id:state.id}) }}"><span class="glyphicon glyphicon-trash"></span></a></td>
    </tr>
    <hr/>
    {% endfor %}
  </tbody>
</table>
{% endblock article %}
