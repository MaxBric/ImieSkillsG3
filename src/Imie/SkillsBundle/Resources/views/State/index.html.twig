{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
<h2>Liste des étapes</h2>
<hr/>
<a href="{{ path('imie_skills_state_add') }}">Ajouter une étape</a>
<hr/>
<table>
  <thead>
    <td>Nom</td>
  </thead>
  <tbody>
    {% for state in states %}
    <tr>
      <td>{{ state.statut }}</a></td>
      <td><a href="{{ path('imie_skills_state_modify', {id:state.id}) }}">Modifier</a></td>
      <td><a href="{{ path('imie_skills_state_delete', {id:state.id}) }}">Supprimer</a></td>
      {% endfor %}
    </tbody>
  </table>
  <hr/>
  {% endblock article %}
