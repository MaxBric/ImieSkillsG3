{% extends 'ImieSkillsBundle::layout.html.twig' %}

{% block article %}
<h2><span class="glyphicon glyphicon-education"></span> Ajout d'une promotion</h2>
<hr/>
<a href="{{ path('imie_skills_promo_index') }}"><span class="glyphicon glyphicon-menu-left"> Retour</a>
  <hr/>
  <form method="post" action="{{ path('imie_skills_promo_add') }}" {{ form_enctype(form) }}>
    {{ form(form) }}
  </form>
  {% endblock article %}
